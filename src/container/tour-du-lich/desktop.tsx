import { ITranslation } from '@/interfaces/ITranslation';
import React from 'react';

interface IContainerTourDesktopProps {
  translation: ITranslation;
}

const ContainerTourDesktop = (props: IContainerTourDesktopProps) => {
  const { translation } = props;
  return <div></div>;
};

export default ContainerTourDesktop;
